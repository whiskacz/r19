import React from 'react'

export default function FormGroup({ nameAmmount, onChange }) {
  return (
    <form className='form' >
        <label>{nameAmmount}</label>
        <input type="number" onChange= />
    </form>
  )
}
